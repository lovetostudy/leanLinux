ctrl+L  清屏可直接清屏而不影响正在输的命令（如开了sslocal后用更方便，可	以不用clear啦
ctrl+C  敲错命令后直接换行而不用删除全部（方便）
ctrl+D 	等价于exit
ctrl+A	等于home键，将光标移动到字符串最前面
ctrl+E	等于end键，将光标移动到字符串最后面
ctrl+U	删除光标前面的所有字符
ctrl+K 	删除光标后面的所有字符
ctrl+R	搜索历史命令（强），利用关键字

alt+. 或 ESC .(不用按住ESC不放） 或 !$(dollar表示最后一个)  引用上一命     	令的最后一个参数

#	表示注释

历史命令：
history	查询历史命令
!n	执行第n条历史命令(n表示历史命令的标号)
!字符串	搜索历史命令中最近一个以xx字符'开头'的命令，如!ser
cd	无参数可自接会home

yum -y install sl 	小火车（无聊的时候可以看一下） sl 直接执行

type -a ll 	查看命令的类型，可以查看一个命令是否为别名（ll 为 ls -l 		的别名《系统制定》）

别名优先（如自己制定一个别名ls，是优先执行自己的别名命令）
\ls	加\跳过别名